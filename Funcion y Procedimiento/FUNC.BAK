
#include <conio.h>
#include <stdio.h>


   int potencia (int a, int b);

void main (){

   int base, exp, p;

   printf ("Valor de la base");
   scanf ("%d",&base);
   printf ("Valor del exponente");
   scanf ("%d",&exp);
   p = potencia (base,exp);
   printf ("La potencia de %d elevado a %d es: %d",base,exp,p);

   getche ();
}

int potencia (int a, int b){
   int x = 1;
   for (i=1;i<=b;i++){
      x = x*a;
   }
   return x;
}
